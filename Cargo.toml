[package]
authors = ["Cole Lawrence <cole@colelawrence.com>"]
edition = "2018"
name = "shipyard_app"
version = "0.1.0"

[features]
default = []
wasm = ["shipyard/wasm"]

[dependencies]
shipyard = {version = "*", path = "../shipyard", features = [], default-features = false}
tracing = "0.1"
